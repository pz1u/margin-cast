# MarginCast Project Instructions

## 프로젝트 개요

이 저장소는 원티드 해커톤을 위한 MarginCast 프로젝트다.

MarginCast는 소상공인의 매장 데이터를 기반으로  
가격, 할인, 세트 구성 등의 경영 선택을 실제 적용 전에 시뮬레이션하고,  
예상 판매량, 기대 이익, 위험도와 불확실성을 제공하는  
AI 기반 경영 의사결정 지원 서비스다.

핵심 질문은 다음과 같다.

> "이 선택을 실제로 적용하면 우리 가게의 판매량과 이익은 어떻게 변할까?"

MarginCast는 단순한 매출 분석이나 AI 상담 서비스가 아니라,  
여러 경영 대안을 데이터 기반으로 비교하고  
실행 전에 결과를 시뮬레이션하는 Decision Engine을 지향한다.

---

## 프로젝트 작업 원칙

현재 요청된 작업 범위를 존중한다.

사용자가 요청하지 않은 기능을 임의로 추가하거나  
대규모 리팩터링, 기술 스택 변경, 아키텍처 확장을 수행하지 않는다.

한 번에 많은 기능을 구현하기보다  
검증 가능한 작은 단위로 나누어 진행한다.

각 작업은 가능한 한 다음 흐름을 따른다.

```text
현재 상태 확인
→ 작업 범위 정의
→ 구현
→ 실행/테스트
→ 결과 검증
→ 필요한 경우 Obsidian 기록
→ Git 커밋
→ 다음 작업
```

코드가 단순히 실행된다는 이유만으로 작업을 완료 처리하지 않는다.

MarginCast의 핵심은 시뮬레이션 결과의 신뢰성이므로,  
데이터와 계산 결과가 의도한 관계를 실제로 나타내는지 확인한다.

---

# Git 작업 규칙

## 논리적 단위 커밋

작업은 의미 있는 논리적 단위로 나누어 커밋한다.

하나의 커밋에는 하나의 목적만 포함한다.

좋은 예:

- 데이터 생성 기본 구조 구현
- 가격탄력성 기반 수요 계산 추가
- 프로모션 효과 생성 로직 추가
- 세트메뉴 생성 규칙 추가
- 데이터 검증 로직 추가
- Monte Carlo 시뮬레이션 구현

나쁜 예:

- 데이터 생성 + 모델 구현 + README 수정 + UI 작업을 하나의 커밋에 포함
- 여러 기능을 구현한 뒤 마지막에 한 번에 커밋
- 파일 하나를 수정할 때마다 의미 없이 커밋

대규모 작업이 끝날 때까지 기다렸다가 한 번에 커밋하지 않는다.

반대로 작은 파일 변경 하나마다 기계적으로 커밋하지 않는다.

기능적으로 설명 가능한 최소 논리 단위를 기준으로 커밋한다.

---

## 커밋 메시지

커밋 메시지는 반드시 한국어로 작성한다.

Conventional Commit 형태를 사용한다.

형식:

```text
<type>: <한국어 설명>
```

사용 가능한 type:

- `feat`: 기능 추가
- `fix`: 버그 수정
- `refactor`: 구조 개선
- `test`: 테스트 추가 또는 수정
- `docs`: 문서 변경
- `chore`: 설정 및 기타 작업
- `data`: 데이터 생성 또는 데이터셋 변경

예시:

```text
feat: 가격탄력성 기반 수요 생성 로직 구현

data: 90일 주문 데이터 생성 규칙 추가

test: 가격 인상 시 판매량 감소 검증 추가

refactor: 주문 생성 로직을 역할별 함수로 분리

docs: 시뮬레이션 데이터 구조 문서화

fix: 배달 수수료 중복 계산 오류 수정
```

다음과 같이 의미가 불분명한 커밋 메시지는 사용하지 않는다.

```text
수정
작업
업데이트
변경사항 반영
fix stuff
modify
update
```

커밋 메시지만 읽어도  
해당 변경사항의 목적을 이해할 수 있어야 한다.

---

## 커밋 전 검증

커밋하기 전에 반드시 해당 변경사항을 검증한다.

가능하면 다음을 수행한다.

1. 관련 코드 실행
2. 관련 테스트 실행
3. 생성 데이터 또는 출력 확인
4. 주요 통계나 결과 확인
5. `git diff` 확인
6. `git status` 확인
7. 의도하지 않은 파일이 포함되지 않았는지 확인

테스트 또는 검증이 실패한 상태를  
정상적으로 완료된 작업처럼 커밋하지 않는다.

실패한 상태 자체를 보존해야 하는 특별한 이유가 있다면  
그 이유를 명시한다.

---

## Push 규칙

논리적 작업 단위가 완료되면  
로컬 Git 커밋까지 수행할 수 있다.

원격 저장소로의 `push`는  
사용자가 명시적으로 요청한 경우에만 수행한다.

다음과 같이 Git 이력이나 원격 저장소에 큰 영향을 줄 수 있는 작업은  
사용자의 명시적인 요청 없이 수행하지 않는다.

- force push
- `git reset --hard`
- 기존 커밋을 변경하는 rebase
- 원격 브랜치 삭제
- 대규모 히스토리 변경

기존 사용자의 Git 변경사항을 임의로 덮어쓰거나 삭제하지 않는다.

---

# Obsidian 개발 기록

MarginCast 프로젝트를 진행하는 동안 Obsidian을  
단순 작업일지가 아니라

**학습 내용, 기술적 발견, 시행착오, 설계 결정의 기록 저장소**

로 활용한다.

Obsidian MCP 또는 다른 Obsidian 연동 도구를 사용할 수 있는 경우,  
개발 과정에서 의미 있는 사건이 발생했을 때 능동적으로 기록한다.

모든 작업이 끝날 때까지 기다릴 필요는 없다.

개발 도중 중요한 사실을 발견했다면  
해당 논리적 작업 단위가 정리되고 검증된 시점에 기록한다.

---

## 기록을 고려해야 하는 경우

다음과 같은 일이 발생하면 Obsidian 기록을 고려한다.

- 처음 알게 된 기술 개념이나 원리
- 중요한 아키텍처 또는 설계 결정
- 특정 기술이나 알고리즘을 선택한 이유
- 여러 구현 방법을 비교하고 하나를 선택한 경우
- 예상과 실제 결과가 다르게 나온 경우
- 디버깅 과정에서 찾기 어려운 원인을 발견한 경우
- 실패한 접근과 그 실패 이유
- 모델링 과정에서 중요한 가정을 발견한 경우
- 데이터 생성 또는 분석 과정에서 특이한 현상을 발견한 경우
- 성능, 정확도, 신뢰도에 영향을 주는 trade-off
- 모델 또는 시뮬레이션의 한계
- 추후 다시 확인해야 할 기술적 위험
- 포트폴리오에서 설명할 가치가 있는 구현
- 기술 면접에서 설명할 가치가 있는 경험
- 프로젝트의 방향에 영향을 줄 정도의 중요한 발견

특히 다음 질문 중 하나라도 `Yes`라면 기록할 가치가 있는지 검토한다.

1. 나중에 같은 문제를 만났을 때 이 기록이 도움이 되는가?
2. 미래의 내가 "왜 이렇게 구현했지?"라고 궁금해할 가능성이 있는가?
3. 이번 작업을 통해 새롭게 이해한 개념이 있는가?
4. 예상과 다른 결과에서 중요한 원인을 발견했는가?
5. 포트폴리오나 면접에서 설명할 가치가 있는가?

---

## 기록하지 않는 경우

다음과 같은 사소하거나 반복적인 작업은  
특별한 이유가 없는 한 기록하지 않는다.

- 파일 하나 생성
- 변수명 변경
- 오타 수정
- 단순 패키지 설치
- 반복적인 CRUD 구현
- 의미 없는 설정값 변경
- 특별한 학습이 없는 단순 코드 수정
- 이미 기록한 내용을 반복하는 작업
- 단순 Git 작업
- 특별한 이유 없는 코드 포맷팅

Obsidian을 작업 로그처럼 사용하지 않는다.

목표는

> "오늘 무엇을 했는가"

를 모두 기록하는 것이 아니라,

> "무엇을 발견했고,  
> 왜 그렇게 결정했으며,  
> 무엇을 배웠는가"

를 남기는 것이다.

---

## Obsidian 기록 형식

가능하면 하나의 기록은 다음 구조를 따른다.

```md
# 제목

## 상황

어떤 작업을 진행하다가 이 내용을 발견했는가.

## 문제 / 발견

무엇이 예상과 달랐거나 중요했는가.

## 원인 / 원리

왜 이런 현상이 발생했는가.

## 검토한 방법

어떤 해결 방법이나 대안을 검토했는가.

## 결정

어떤 선택을 했으며,  
다른 방법 대신 이 방법을 선택한 이유는 무엇인가.

## MarginCast 적용

이 내용을 프로젝트에 실제로 어떻게 적용했는가.

## 결과

적용 또는 검증 후 어떤 결과가 나왔는가.

## 배운 점

이번 작업을 통해 이해한 핵심 내용을 짧게 정리한다.

## 참고

관련 코드, 파일, 모델, 커밋 또는 추후 확인할 내용.
```

모든 항목을 억지로 채울 필요는 없다.

내용에 맞지 않는 항목은 생략할 수 있다.

---

## Obsidian 기록 시점

기본 흐름은 다음과 같다.

```text
작업
→ 특이사항 또는 학습 포인트 발견
→ 원인 분석
→ 구현 또는 해결
→ 실제 검증
→ 기록 가치 판단
→ 필요한 경우 Obsidian 기록
→ Git 커밋
```

단, 기록에서 커밋 해시나 커밋 내용을 참조하는 것이 더 유용하다면

```text
검증
→ Git 커밋
→ Obsidian 기록에 커밋 정보 추가
```

순서로 진행해도 된다.

Obsidian 기록과 Git 커밋의 순서를 기계적으로 강제하지 않는다.

중요한 것은

**검증되지 않은 가설을 확정된 사실처럼 기록하지 않는 것**

이다.

문제 해결 도중 아직 결론이 나지 않은 내용은  
필요하면 임시 메모로 남길 수 있지만,  
검증 후 내용을 업데이트한다.

---

## Obsidian 연동 원칙

Obsidian MCP 또는 다른 Obsidian 연동 도구가 사용 가능하면  
가능한 범위에서 직접 기록한다.

현재 사용 가능한 Vault나  
MarginCast 프로젝트 기록 위치가 명확하지 않다면  
경로를 임의로 추측하지 않는다.

새로운 Vault도 임의로 생성하지 않는다.

저장 위치가 불분명한 경우에는

- 사용자에게 저장 위치를 확인하거나
- 기록해야 할 내용을 작업 완료 보고에 별도로 표시한다.

Obsidian 연동 실패 때문에  
개발 작업 자체를 중단하지 않는다.

---

# MarginCast 데이터 및 모델링 원칙

## LLM과 계산 로직 분리

LLM이 다음 핵심 수치를 임의로 생성하게 하지 않는다.

- 예상 판매량
- 기대 이익
- 가격탄력성
- 성공 확률
- 신뢰구간
- 위험도 계산에 필요한 핵심 수치

이러한 값은 명시적인

- 통계 모델
- 머신러닝 모델
- 수식
- 시뮬레이션
- 데이터 기반 계산

을 통해 생성한다.

LLM은 주로 다음 역할을 담당한다.

- 현재 상황 해석
- 경영 전략 후보 생성
- 시뮬레이션 결과 설명
- 전략 비교
- 사용자 친화적인 표현 생성

LLM이 계산 엔진을 대체하지 않는다.

---

## 설명 가능한 결과

MarginCast가 제공하는 예측에는  
가능한 경우 다음 정보를 함께 제공한다.

- 예상값
- 근거 데이터
- 사용된 모델 또는 계산 방식
- 예상 범위
- 신뢰도
- 성공 확률
- 주요 가정

사용자에게 하나의 숫자를  
확정적인 미래처럼 보여주지 않는다.

---

## 불확실성

미래 예측에는 항상 불확실성이 존재한다고 가정한다.

데이터가 부족한 경우  
억지로 높은 신뢰도의 결론을 만들지 않는다.

필요하면 다음과 같이 명확하게 표현한다.

```text
예측 신뢰도 낮음
가격 변경 이력이 부족합니다.
작은 실험을 통해 추가 데이터를 확보하는 것을 권장합니다.
```

데이터가 부족한 상황을 숨기는 것보다  
불확실성을 투명하게 표시하는 것을 우선한다.

---

## Synthetic Data 원칙

Synthetic Dataset은 단순 랜덤 값의 집합으로 만들지 않는다.

각 데이터에는 명시적인 생성 규칙과 Ground Truth가 존재해야 한다.

예를 들어 다음 관계가 실제 데이터에 반영되어야 한다.

- 가격 상승 → 수요 감소
- 할인 → 수요 증가
- 점심/저녁 → 주문 증가
- 비 오는 날 → 배달 비중 증가
- 원가 상승 → 기여이익률 감소
- 연관성이 높은 메뉴 → 동시구매 증가
- 세트메뉴 → 신규 수요 + 기존 단품 잠식

가능하면 Random Seed를 고정하여  
동일한 조건에서 동일한 데이터를 재현할 수 있게 한다.

Ground Truth는 모델 학습 입력과 분리하고  
추정 결과 검증 용도로 사용한다.

---

## 데이터 누수 방지

시계열 데이터는 특별한 이유가 없는 한  
랜덤하게 Train/Test Split하지 않는다.

시간 순서를 유지하여

```text
과거 → Train
중간 → Validation
미래 → Test
```

구조를 사용한다.

미래 데이터를 이용해 과거를 예측하는  
Data Leakage가 발생하지 않도록 주의한다.

---

## 검증 우선

새로운 모델이나 시뮬레이션 기능을 구현하면  
단순히 코드 실행 여부만 확인하지 않는다.

예를 들어 Synthetic Dataset 관련 작업이라면 실제로 다음을 확인한다.

- 가격 상승 시 판매량이 감소하는가
- 가격 인하 시 판매량이 증가하는가
- 할인 기간 판매량이 증가하는가
- 점심/저녁 피크가 나타나는가
- 비 오는 날 배달 비중이 증가하는가
- 원가 상승 이후 기여이익률이 감소하는가
- 의도한 메뉴 사이에 구매 연관성이 나타나는가

가능하면 결과를 숫자로 확인한다.

---

# 코드 작성 원칙

## 단순성과 가독성

해커톤 프로젝트라는 점을 고려하여  
과도한 추상화와 불필요한 엔터프라이즈 패턴을 피한다.

다만 핵심 로직은 테스트 가능하도록  
역할별 함수나 모듈로 분리한다.

짧은 코드보다  
이해하기 쉬운 코드를 우선한다.

---

## 주석

코드 자체로 이해 가능한 내용을  
불필요하게 주석으로 반복하지 않는다.

다음과 같은 경우에는 주석을 남긴다.

- 통계적 가정
- 비즈니스 규칙
- 복잡한 계산식
- 직관적이지 않은 구현 이유
- MarginCast에 특화된 모델링 결정

주석은 가능하면

**무엇을 하는지**보다  
**왜 이렇게 하는지**

를 설명한다.

---

## 테스트

핵심 계산 로직은 가능한 경우 테스트를 작성한다.

특히 다음과 같은 영역은 우선적으로 검증한다.

- 가격 변화 계산
- 가격탄력성 적용
- 프로모션 효과
- 기여이익 계산
- 수수료 계산
- 세트메뉴 cannibalization
- Monte Carlo Simulation
- 데이터 생성 재현성

구현 세부사항보다  
실제 기대 동작을 검증하는 테스트를 우선한다.

---

# ECC 활용

ECC 스킬을 사용할 수 있는 환경에서는  
현재 작업에 실제로 도움이 되는 스킬만 선택적으로 활용한다.

스킬을 사용할 수 있다는 이유만으로  
불필요한 도구, 프레임워크 또는 프로세스를 추가하지 않는다.

MarginCast의 작업 범위와 단순성을 우선한다.

---

# 작업 완료 보고

하나의 논리적 작업 단위를 완료하면  
가능하면 다음 내용을 간단하게 보고한다.

- 무엇을 구현했는가
- 어떤 방식으로 검증했는가
- 중요한 결과 수치는 무엇인가
- 생성한 Git 커밋
- Obsidian에 기록한 내용이 있는가
- 발견된 문제나 남아 있는 위험이 있는가
- 다음으로 자연스럽게 진행할 작업은 무엇인가

단순한 변경 파일 목록보다  
기능적 결과와 검증 내용을 우선해서 설명한다.

---

# 협업 역할과 Obsidian 저장 위치

MarginCast 서비스의 AI 에이전트 설계와 구현은 사용자가 주도한다.
Codex는 요청된 범위의 데이터, 모델, 계산 엔진, 검증 작업을 진행한다.
서비스 AI 에이전트의 선행 구현은 하지 않으며, 사용자가 요청하면
설명, 코드 리뷰, 디버깅, 계산 엔진과의 연결을 지원한다.
에이전트와 계산 엔진 사이의 입력·출력 형식은 통합 단계에서 함께 정한다.

확인된 기존 Obsidian Vault는 `C:\workspace\jiu`다.
MarginCast 기록은 `C:\workspace\jiu\10_프로젝트\MarginCast`에 저장한다.
기존 노트는 확인 후 필요한 부분만 갱신하며 무관한 내용을 덮어쓰지 않는다.
